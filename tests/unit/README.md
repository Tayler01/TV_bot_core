# Unit Tests

Workspace crate tests currently cover the Phase 0 and Phase 1 foundations:

- config loading and env overrides
- runtime mode transitions and readiness checks
- strict strategy parsing, validation, and compilation
- journal append/list behavior

Future unit coverage should expand here as symbol resolution, risk sizing, and latency metric primitives are added.
