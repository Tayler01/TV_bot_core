# Phase 0 / Phase 1 Foundations

This repository is scaffolded as a Rust workspace with explicit crate boundaries that match the architecture plan in `AGENTS.md` and `codex_futures_bot_plan.md`.

Implemented foundations:

- `crates/core_types`: shared runtime-safe domain contracts such as `CompiledStrategy`, `RuntimeMode`, `ExecutionIntent`, `RiskDecision`, `ArmReadinessReport`, and `EventJournalRecord`
- `crates/config`: TOML plus environment-based runtime configuration loading with explicit startup mode validation
- `crates/runtime_kernel`: mode transitions, warmup state, arming gates, readiness report evaluation, a runtime orchestration bridge that evaluates risk before execution dispatch and journals every risk decision and hard-override event, and a unified command loop so manual actions and strategy intents share the same audited execution path
- `crates/strategy_loader`: strict Markdown section parsing, YAML validation, and compilation into `CompiledStrategy`
- `crates/indicators`: built-in strategy-agnostic indicator primitives for OHLCV-derived calculations such as SMA/EMA, breakout ranges, volume baselines, and candle-shape helpers
- `crates/rule_engine`: parsing and evaluation of built-in rule references, with strict condition compilation, timeframe/warmup validation hooks, and signal-plan combination modes for long/short evaluation
- `crates/strategy_runtime`: runtime-safe compilation of validated strategies into executable rule/session plans plus an edge-triggered strategy evaluator that turns market snapshots into normalized `SignalDecision` values and `ExecutionIntent` requests without leaking strategy logic into risk or execution
- `crates/instrument_resolver`: strategy-agnostic front-month resolution from market family into explicit Databento and Tradovate symbol mappings
- `crates/market_data`: Databento-oriented subscription contracts, async session/reconnect transport boundaries, a concrete live transport backed by the official Databento Rust client, a runtime-facing market-data service loop for manual warmup/replay handling, feed health states, rolling buffers, local multi-timeframe aggregation, warmup tracking, and runtime-ready status snapshots
- `crates/broker_tradovate`: Tradovate auth/session foundations with explicit credential and environment contracts, centralized access-token renewal, account lookup and routing-aware selection, user-sync session management, reconnect/review state tracking, provider-safe broker status snapshots, fill/account reconciliation from live sync, and a concrete live REST/WebSocket transport for bearer auth, renewal, `authorize`, `user/syncrequest`, `props` updates, heartbeat handling, `placeorder`, `placeoso`, and `liquidateposition`
- `crates/execution_engine`: strategy-agnostic execution planning and dispatch that turns normalized `ExecutionIntent` values plus runtime/broker context into explicit Tradovate order primitives, flatten-first or direct-reverse plans, broker-side bracketed entries, session-managed Tradovate submission flows, and safety-blocking errors when arming/readiness or strategy execution settings do not permit order placement
- `crates/risk_engine`: strategy-agnostic pre-execution risk evaluation that normalizes fixed and risk-based sizing, enforces daily-trade / consecutive-loss / unrealized-drawdown limits, and surfaces broker-required protection gaps as warning or temporary hard-override decisions
- `crates/control_api`: a transport-agnostic local control surface with thin HTTP-style command handlers and a WebSocket-style event hub, mapping dashboard/CLI manual commands and strategy intents onto the unified runtime command loop so control-plane transports stay thin without bypassing auditing or risk checks
- `crates/journal`: testable event-journal abstraction with in-memory, persistence-backed, and projection-updating adapters
- `crates/persistence`: Postgres-first persistence planning, safe SQLite-fallback runtime selection, and real Postgres/SQLite plus in-memory storage adapters for event, health, trade-latency, strategy-run, order, fill, position, PnL-snapshot, and trade-summary records
- `crates/metrics`: deterministic trade-path latency calculations with strict timestamp ordering checks plus a runtime latency collector that persists and snapshots the latest recorded trade path
- `crates/health`: runtime health supervision with durable health snapshots, runtime error counting, DB-write latency tracking, queue-lag hooks, and cross-platform current-process CPU/memory sampling
- `crates/state_store`: event-sourced runtime-state projection that can rebuild from journal records and stay updated as new records are appended, plus a queryable trading-history projection for active runs, working orders, open positions, open trades, and aggregate trade PnL/fees/commissions/slippage
- `apps/runtime`: a real local runtime host that binds HTTP and WebSocket listeners, serves `/health`, `/status`, `/readiness`, `/history`, `/runtime/commands`, `/commands`, and `/events`, maintains one loaded compiled strategy in process, reuses the audited control API path, projects live broker account/sync/position state into status and readiness, continuously syncs broker snapshots into the durable trading-history projection, and now blocks shutdown with open positions until the operator explicitly chooses flatten-first or leave-broker-protected behavior
- `apps/cli`: a real local operator CLI that can launch the runtime, load a strategy, trigger warmup, switch mode, arm/disarm with confirmations, resolve reconnect review, review shutdown choices, flatten by contract id, and inspect status, readiness, history, and sampled runtime resource health through the local control API

Reserved crate boundaries are scaffolded for later phases so market data, broker integration, execution, risk, control API, and persistence can be added without violating the strategy-agnostic execution core.

Important guardrails already enforced:

- runtime mode is explicit and required at startup
- trading cannot arm from `observation` or `paused`
- warmup readiness is separate from arming
- strategy runtime truth comes from compiled internal objects, not raw Markdown
- unknown Markdown sections and unknown YAML fields fail validation
- Databento reconnect state, heartbeat visibility, and disconnect reasons are explicit in runtime snapshots
- Databento live transport decodes official DBN trade/bar/system records into internal `MarketEvent` updates without leaking provider-specific details into the execution core
- manual warmup can run either live-only or replay-then-live, and replay catch-up must complete before the market-data service reports trade readiness
- multi-timeframe warmup can be satisfied from the smallest provider-supported bar feed without pushing strategy-specific logic into the execution core
- broker account selection and broker-sync readiness now flow through generic broker status snapshots, so the runtime can block unsafe paper/live mismatches, stale sync, mismatch states, and reconnect review requirements without learning Tradovate-specific internals
- runtime-host status/readiness now consume those same broker snapshots directly, so current account projection and flatten/manual command context come from live session state instead of host-local placeholders
- Tradovate networking is isolated behind broker traits, with HTTP token/account operations, provider-specific order submission primitives, and WebSocket sync parsing confined to `broker_tradovate` so the execution core still consumes only normalized broker state
- broker sync now normalizes fills and account-state snapshots alongside positions and working orders, which keeps reconciliation provider-aware while preserving shared runtime contracts
- execution planning and dispatch remain strategy-agnostic while still producing broker-native order structures, so reversal mode, scaling permissions, broker-required stop/TP expectations, and no-new-entry safety checks are enforced without embedding signal logic into the broker adapter
- broker-preferred execution hints surface as warnings instead of silently changing behavior
- risk evaluation now adjusts entry quantities from compiled sizing rules before execution, and broker-required stop / take-profit / trailing / daily-loss gaps are surfaced as explicit override decisions instead of being silently ignored
- runtime orchestration now runs `risk_engine` directly ahead of broker dispatch and persists both the risk decision and any hard-override requirement/use into the event journal for auditability
- manual and strategy-originated intents now enter through the same runtime control loop, which normalizes strategy provenance, journals the incoming intent, and records whether dispatch was performed, skipped, or failed
- the local control API now routes manual dashboard/CLI commands and strategy-originated intents into that same runtime loop, with HTTP-style response mapping and WebSocket event publication layers that do not need their own execution path
- the runtime app now hosts those HTTP/WebSocket transports directly, keeps startup independent from broker credentials by exposing a clear unavailable state instead of failing the local control plane outright, and owns the loaded compiled strategy plus lifecycle state used by CLI/dashboard controls
- host-side command handling now rewrites client execution requests with server-side runtime mode, arm/override state, and the loaded compiled strategy before dispatch so the control plane does not trust raw client runtime gating inputs
- the runtime host now refreshes the Databento service on a background interval, so `/status`, `/readiness`, and warmup synchronization do not depend on manual polling to observe feed-state changes
- storage policy now comes from a shared persistence runtime-selection foundation instead of host-local branching, and the runtime host now reports the active durable backend, fallback activation state, and journal durability explicitly
- runtime journaling now flows through a persistence-backed projecting journal, so durable event writes and live event-sourced state projection share one audited append path
- durable trading-history storage now has provider-agnostic record contracts for runs, orders, fills, positions, PnL snapshots, and closed/open trade summaries across in-memory, SQLite, and Postgres backends
- state projection now includes a reusable trading-history view that can rebuild from persisted records and answer query-style questions about active runs, working orders, open positions, open trades, and aggregate realized trade outcomes
- runtime-host history wiring now keeps those trading-history stores updated from live broker snapshots and exposes the projection to operator tooling through `/history` and CLI history inspection
- runtime-host observability now persists trade-path latency and system-health snapshots, publishes health and trade-latency WebSocket events, and surfaces latest observability state through `/health` and `/status`
- runtime-host health capture now samples current-process CPU and resident memory on a cross-platform path, persists those samples in the existing health store, and projects them through `/health`, `/status`, and the CLI operator view
- strict strategies now compile a second time into runtime-ready rule/session objects, so built-in condition references, entry-condition JSON, condition timeframe declarations, and warmup sufficiency are validated before any signal evaluation starts
- strategy evaluation is now edge-triggered and strategy-agnostic, producing normalized entry/exit/flatten/pause intents from market snapshots while keeping signal logic out of the broker, risk, and control layers
- degraded data or broker sync continue to block new entries, that no-new-entry gate is now enforced server-side during command sanitization, and reconnect-review or broker-sync-mismatch failsafes can now pause the strategy before execution is considered
- execution-planning failures that represent safety or readiness conflicts now map to operator-facing HTTP conflict/precondition responses instead of surfacing as generic internal-server errors
- reconnect/open-position recovery now has an explicit operator review path through `/runtime/commands` and the CLI, including safe acknowledge-vs-close behavior that still reuses the audited flatten path when position closure is chosen
- shutdown with open positions now blocks on signal by default, projects pending review state through `/status`, and requires an explicit flatten-first or leave-broker-protected decision before the host exits

Verification status:

- `cargo test -j 1` passed for the full workspace after the broker execution/reconciliation changes landed.
- `cargo test -j 1 -p tv-bot-broker-tradovate` passes after formatting, covering live REST auth/account/execution calls, WebSocket authorize and `user/syncrequest` flow, fill/account `props` reconciliation, reconnect review gating, and session-managed order submission context.
- `cargo test -j 1 -p tv-bot-execution-engine` passes after formatting, covering arm-gated order blocking, market and limit entry planning, broker-side OSO bracket construction, flatten-first reversal, direct-reverse sizing, scale-in blocking, safe flatten no-op behavior, dispatch through the Tradovate session manager, and planning-vs-broker error separation.
- `cargo test -j 1 -p tv-bot-risk-engine` passes after formatting, covering fixed and risk-based sizing, daily-trade and consecutive-loss limits, unrealized-drawdown blocking, broker-required override paths, and broker-preferred warning-only behavior.
- `cargo test -j 1 -p tv-bot-execution-engine -p tv-bot-risk-engine -p tv-bot-runtime-kernel` passes after formatting, covering runtime orchestration of risk evaluation before execution dispatch, quantity adjustment propagation into broker-native order placement, rejected/override-gated no-dispatch behavior, journaling of both risk decisions and hard overrides, and the unified manual/strategy command path into audited execution.
- `cargo test -j 1 -p tv-bot-control-api -p tv-bot-runtime-kernel` passes after formatting, covering control-plane mapping of dashboard and strategy commands onto the unified runtime loop, HTTP-style status translation for executed vs override-required vs internal-error command outcomes, and WebSocket-style event publication for live command results.
- `cargo test -j 1 -p tv-bot-runtime` passes when run against a fresh target directory, covering runtime-host strategy load state, readiness/override behavior, flatten-request construction, HTTP `/status` and `/commands` routes, and safe startup into a dispatch-unavailable mode when broker configuration is incomplete.
- `cargo test -j 1 -p tv-bot-control-api -p tv-bot-runtime -p tv-bot-cli` passes when run against a fresh target directory, covering the shared control-plane contracts, broker-backed status projection through the runtime host, and the CLI argument surface for launch and lifecycle commands.
- `cargo test -j 1 -p tv-bot-runtime -p tv-bot-cli -p tv-bot-control-api -p tv-bot-execution-engine` passes when run against a fresh target directory, covering reconnect-review acknowledgement, shutdown blocking when broker-side protection is missing, shutdown flatten-review state transitions, the CLI reconnect/shutdown command surface, and flatten safety availability even when normal order placement is otherwise blocked.
- `cargo test -j 1 -p tv-bot-indicators -p tv-bot-rule-engine -p tv-bot-strategy-runtime` passes when run against a fresh target directory, covering indicator primitives, strict built-in condition parsing, signal-plan evaluation, runtime compilation of entry/session rules, edge-triggered entry emission, opposite-signal exits, reconnect-review pauses, and session-end flatten behavior.
- `cargo test -j 1 -p tv-bot-persistence -p tv-bot-metrics -p tv-bot-market-data -p tv-bot-control-api -p tv-bot-runtime -p tv-bot-cli` passes when run against a fresh target directory, covering the shared persistence plan, in-memory event/health/latency storage adapters, deterministic trade-path latency calculations, live market-data status projection through the runtime host, and the control/CLI surfaces that consume those snapshots.
- `cargo test -j 1 -p tv-bot-persistence -p tv-bot-state-store -p tv-bot-journal -p tv-bot-runtime -p tv-bot-cli` passes when run against a fresh target directory, covering real SQLite durable persistence, runtime backend fallback selection, persistent-journal delegation, event-sourced state projection hydration, and runtime-host selection of a durable SQLite journal backend.
- `cargo test -j 1 -p tv-bot-persistence -p tv-bot-state-store -p tv-bot-journal -p tv-bot-control-api -p tv-bot-runtime -p tv-bot-cli` passes when run against a fresh target directory, covering durable strategy-run/order/fill/position/PnL/trade-summary persistence, queryable trading-history projection rebuilds, persistent-journal state hydration, and the downstream runtime/control surfaces that depend on those crates.
- `cargo test -j 1 -p tv-bot-broker-tradovate -p tv-bot-control-api -p tv-bot-runtime -p tv-bot-cli` passes when run against a fresh target directory, covering Tradovate live auth/sync/execution transport, runtime-host history projection sync and `/history` routing, CLI history inspection, and the control-plane surfaces that depend on those broker/runtime paths.
- `cargo test -j 1 -p tv-bot-metrics -p tv-bot-health -p tv-bot-control-api -p tv-bot-execution-engine -p tv-bot-runtime -p tv-bot-cli` passes when run against a fresh target directory, covering runtime-collected latency persistence, durable health supervision, operator-facing HTTP mapping of execution-planning conflicts, server-side degraded-entry blocking, runtime-host `/health` and `/status` observability projection, and positive scale-in planning when explicitly enabled.
- `cargo test -j 1 -p tv-bot-health -p tv-bot-runtime -p tv-bot-cli` passes when run against a fresh target directory, covering persisted health snapshots, cross-platform runtime resource sampling projection through `/health` and `/status`, and the CLI operator view for sampled CPU and memory telemetry.
- This workspace lives under Windows + OneDrive, so subsequent full-workspace reruns may still hit transient `LNK1104` file-locking on generated test executables. The current post-change full-workspace rerun reproduced that linker lock in unrelated `broker_tradovate` test executables, so serial targeted verification remains the safest retry path when that happens.
